# Components — ui/

shadcn/ui-compatible primitives (Radix + tokens) will land here in Phase 1.2+.

Phase 1.1 configures `components.json` and Radix/CVA dependencies only — no premature component dump.
